/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { CrmCore } from "../core.js";
import { encodeFormQuery } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { resolveSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { CrmError } from "../models/errors/crm-error.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../models/errors/http-client-errors.js";
import { ResponseValidationError } from "../models/errors/response-validation-error.js";
import { SDKValidationError } from "../models/errors/sdk-validation-error.js";
import * as operations from "../models/operations/index.js";
import { APICall, APIPromise } from "../types/async.js";
import { Result } from "../types/fp.js";

/**
 * Search Products
 *
 * @remarks
 * Retrieves a list of products that can be purchased by customers through an order, filtered by their category, type etc. Modifier and Variant products are excluded since these product can only be ordered as part of another product and not individually.
 */
export function productCatalogueComCrmProductSelfServiceResourceListProducts(
  client: CrmCore,
  security: operations.ComCrmProductSelfServiceResourceListProductsSecurity,
  request?:
    | operations.ComCrmProductSelfServiceResourceListProductsRequest
    | undefined,
  options?: RequestOptions,
): APIPromise<
  Result<
    operations.ComCrmProductSelfServiceResourceListProductsResponse,
    | CrmError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >
> {
  return new APIPromise($do(
    client,
    security,
    request,
    options,
  ));
}

async function $do(
  client: CrmCore,
  security: operations.ComCrmProductSelfServiceResourceListProductsSecurity,
  request?:
    | operations.ComCrmProductSelfServiceResourceListProductsRequest
    | undefined,
  options?: RequestOptions,
): Promise<
  [
    Result<
      operations.ComCrmProductSelfServiceResourceListProductsResponse,
      | CrmError
      | ResponseValidationError
      | ConnectionError
      | RequestAbortedError
      | RequestTimeoutError
      | InvalidRequestError
      | UnexpectedClientError
      | SDKValidationError
    >,
    APICall,
  ]
> {
  const parsed = safeParse(
    request,
    (value) =>
      z.parse(
        z.optional(
          operations
            .ComCrmProductSelfServiceResourceListProductsRequest$outboundSchema,
        ),
        value,
      ),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return [parsed, { status: "invalid" }];
  }
  const payload = parsed.value;
  const body = null;

  const path = pathToFunc("/self-service/v2/products")();

  const query = encodeFormQuery({
    "billing_period": payload?.billing_period,
    "billing_period_uot": payload?.billing_period_uot,
    "brand_id": payload?.brand_id,
    "category_id": payload?.category_id,
    "category_id_exact": payload?.category_id_exact,
    "characteristics": payload?.characteristics,
    "classification": payload?.classification,
    "composition": payload?.composition,
    "country": payload?.country,
    "currency": payload?.currency,
    "custom_fields": payload?.custom_fields,
    "family_id": payload?.family_id,
    "fulfilled_by": payload?.fulfilled_by,
    "include_characteristics": payload?.include_characteristics,
    "include_custom_fields": payload?.include_custom_fields,
    "include_total": payload?.include_total,
    "includes_one_time_services": payload?.includes_one_time_services,
    "instance_model": payload?.instance_model,
    "is_modifier": payload?.is_modifier,
    "name": payload?.name,
    "order": payload?.order,
    "order_catalog_id": payload?.order_catalog_id,
    "order_category_id": payload?.order_category_id,
    "owned_by": payload?.owned_by,
    "page": payload?.page,
    "product_ids": payload?.product_ids,
    "product_skus": payload?.product_skus,
    "search_value": payload?.search_value,
    "size": payload?.size,
    "sort": payload?.sort,
    "supply_method": payload?.supply_method,
    "type_id": payload?.type_id,
    "validity_date": payload?.validity_date,
  });

  const headers = new Headers(compactMap({
    Accept: "application/json",
  }));

  const requestSecurity = resolveSecurity(
    [
      {
        fieldName: "api_key",
        type: "apiKey:header",
        value: security?.secretAPIKey,
      },
    ],
    [
      {
        fieldName: "Authorization",
        type: "http:bearer",
        value: security?.authorizationSelfService,
      },
    ],
  );

  const context = {
    options: client._options,
    baseURL: options?.serverURL ?? client._baseURL ?? "",
    operationID: "com.crm.ProductSelfServiceResource_listProducts",
    oAuth2Scopes: null,

    resolvedSecurity: requestSecurity,

    securitySource: security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: options?.serverURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    userAgent: client._options.userAgent,
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return [requestRes, { status: "invalid" }];
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: [
      "400",
      "401",
      "403",
      "404",
      "4XX",
      "500",
      "502",
      "503",
      "504",
      "5XX",
    ],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return [doResult, { status: "request-error", request: req }];
  }
  const response = doResult.value;

  const [result] = await M.match<
    operations.ComCrmProductSelfServiceResourceListProductsResponse,
    | CrmError
    | ResponseValidationError
    | ConnectionError
    | RequestAbortedError
    | RequestTimeoutError
    | InvalidRequestError
    | UnexpectedClientError
    | SDKValidationError
  >(
    M.json(
      200,
      operations
        .ComCrmProductSelfServiceResourceListProductsResponse$inboundSchema,
    ),
    M.fail([400, 401, 403, 404, "4XX"]),
    M.fail([500, 502, 503, 504, "5XX"]),
  )(response, req);
  if (!result.ok) {
    return [result, { status: "complete", request: req, response }];
  }

  return [result, { status: "complete", request: req, response }];
}
